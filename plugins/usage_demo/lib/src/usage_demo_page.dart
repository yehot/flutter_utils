import 'package:flutter/material.dart';

class UsageDemoPage extends StatelessWidget {
  const UsageDemoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Container(

      ),
    );
  }
}
